<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caicai.swe.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.caicai.swe.entity.Product">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="price" property="price"/>
        <result column="category_id" property="categoryId"/>
        <result column="images" property="images"/>
        <result column="status" property="status"/>
        <result column="view_count" property="viewCount"/>
        <result column="favorite_count" property="favoriteCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="VOResultMap" type="com.caicai.swe.vo.ProductVO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="user_avatar" property="userAvatar"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="price" property="price"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="images" property="images"/>
        <result column="status" property="status"/>
        <result column="view_count" property="viewCount"/>
        <result column="favorite_count" property="favoriteCount"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <select id="selectVOById" resultMap="VOResultMap">
        SELECT p.*, u.username, u.avatar as user_avatar, c.name as category_name
        FROM product p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        WHERE p.id = #{id}
    </select>

    <select id="selectList" resultMap="VOResultMap">
        SELECT p.*, u.username, u.avatar as user_avatar, c.name as category_name
        FROM product p
        LEFT JOIN user u ON p.user_id = u.id
        LEFT JOIN category c ON p.category_id = c.id
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <if test="categoryId != null">
                AND p.category_id = #{categoryId}
            </if>
            <if test="userId != null">
                AND p.user_id = #{userId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (p.title LIKE CONCAT('%', #{keyword}, '%')
                OR p.description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY p.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM product p
        <where>
            <if test="status != null">
                AND p.status = #{status}
            </if>
            <if test="categoryId != null">
                AND p.category_id = #{categoryId}
            </if>
            <if test="userId != null">
                AND p.user_id = #{userId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (p.title LIKE CONCAT('%', #{keyword}, '%')
                OR p.description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.caicai.swe.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (user_id, title, description, price, category_id, images, status)
        VALUES (#{userId}, #{title}, #{description}, #{price}, #{categoryId}, #{images}, 1)
    </insert>

    <update id="update" parameterType="com.caicai.swe.entity.Product">
        UPDATE product
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="price != null">price = #{price},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="images != null">images = #{images},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM product WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE product SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <update id="incrementFavoriteCount">
        UPDATE product SET favorite_count = favorite_count + 1 WHERE id = #{id}
    </update>

    <update id="decrementFavoriteCount">
        UPDATE product SET favorite_count = favorite_count - 1 WHERE id = #{id}
    </update>

</mapper>